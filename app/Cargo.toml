[package]
name = "basscript-app"
version = "0.1.5"
edition = "2024"

[features]
default = []
dev-dynamic = ["bevy/dynamic_linking"]

[dependencies]
bevy = "0.18"
basscript-ui = { path = "../ui" }
basscript-core = { path = "../core" }
