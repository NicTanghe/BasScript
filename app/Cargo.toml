[package]
name = "basscript-app"
version = "0.1.1"
edition = "2024"

[dependencies]
bevy = { version = "0.18", features = ["dynamic_linking"] }
basscript-ui = { path = "../ui" }
basscript-core = { path = "../core" }
